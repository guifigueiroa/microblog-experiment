<% if @user.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      :/  Foram encontrados <%= pluralize(@user.errors.count, "erro") %> no formulário.
    </div>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>